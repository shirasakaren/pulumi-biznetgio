// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class BaremetalElasticStorage extends pulumi.CustomResource {
    /**
     * Get an existing BaremetalElasticStorage resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): BaremetalElasticStorage {
        return new BaremetalElasticStorage(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'biznetgio:index:BaremetalElasticStorage';

    /**
     * Returns true if the given object is an instance of BaremetalElasticStorage.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BaremetalElasticStorage {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BaremetalElasticStorage.__pulumiType;
    }

    /**
     * Creation timestamp (alias of created_at/date_created).
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * Billing cycle, e.g. `m` for monthly or `a` for annual. Create-only, changing it replaces the storage.
     */
    declare public readonly cycle: pulumi.Output<string>;
    /**
     * Account id of the target baremetal. Can only be set at creation (no re-attach endpoint).
     */
    declare public readonly metalAccountId: pulumi.Output<number>;
    /**
     * Pay the invoice with the registered credit card. Defaults to true. Set false to leave the invoice unpaid in the portal; the resource stays Pending until paid.
     */
    declare public readonly payWithCreditCard: pulumi.Output<boolean | undefined>;
    /**
     * Product id from `GET /baremetal-neo-elastic-storages/products`. Changing it triggers change-package (`POST .../{account_id}`).
     */
    declare public readonly productId: pulumi.Output<number>;
    /**
     * Promo code to apply at creation.
     */
    declare public readonly promocode: pulumi.Output<string | undefined>;
    /**
     * Raw JSON of the last read response, for anything not modeled yet.
     */
    declare public /*out*/ readonly raw: pulumi.Output<string>;
    /**
     * Storage size in GB. Defaults to 100. Changing it triggers upgrade (`PUT .../{account_id}`) - grow-only, shrinking is rejected by the API.
     */
    declare public readonly size: pulumi.Output<number | undefined>;
    /**
     * Current status of the storage (e.g. Active, Pending, Suspended, Terminated).
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * Name of the storage. Create-only, changing it replaces the storage.
     */
    declare public readonly storageName: pulumi.Output<string>;

    /**
     * Create a BaremetalElasticStorage resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BaremetalElasticStorageArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            if (args?.cycle === undefined && !opts.urn) {
                throw new Error("Missing required property 'cycle'");
            }
            if (args?.metalAccountId === undefined && !opts.urn) {
                throw new Error("Missing required property 'metalAccountId'");
            }
            if (args?.productId === undefined && !opts.urn) {
                throw new Error("Missing required property 'productId'");
            }
            if (args?.storageName === undefined && !opts.urn) {
                throw new Error("Missing required property 'storageName'");
            }
            resourceInputs["cycle"] = args?.cycle;
            resourceInputs["metalAccountId"] = args?.metalAccountId;
            resourceInputs["payWithCreditCard"] = (args?.payWithCreditCard) ?? true;
            resourceInputs["productId"] = args?.productId;
            resourceInputs["promocode"] = args?.promocode;
            resourceInputs["size"] = (args?.size) ?? 100;
            resourceInputs["storageName"] = args?.storageName;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["raw"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        } else {
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["cycle"] = undefined /*out*/;
            resourceInputs["metalAccountId"] = undefined /*out*/;
            resourceInputs["payWithCreditCard"] = undefined /*out*/;
            resourceInputs["productId"] = undefined /*out*/;
            resourceInputs["promocode"] = undefined /*out*/;
            resourceInputs["raw"] = undefined /*out*/;
            resourceInputs["size"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["storageName"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["raw"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(BaremetalElasticStorage.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a BaremetalElasticStorage resource.
 */
export interface BaremetalElasticStorageArgs {
    /**
     * Billing cycle, e.g. `m` for monthly or `a` for annual. Create-only, changing it replaces the storage.
     */
    cycle: pulumi.Input<string>;
    /**
     * Account id of the target baremetal. Can only be set at creation (no re-attach endpoint).
     */
    metalAccountId: pulumi.Input<number>;
    /**
     * Pay the invoice with the registered credit card. Defaults to true. Set false to leave the invoice unpaid in the portal; the resource stays Pending until paid.
     */
    payWithCreditCard?: pulumi.Input<boolean | undefined>;
    /**
     * Product id from `GET /baremetal-neo-elastic-storages/products`. Changing it triggers change-package (`POST .../{account_id}`).
     */
    productId: pulumi.Input<number>;
    /**
     * Promo code to apply at creation.
     */
    promocode?: pulumi.Input<string | undefined>;
    /**
     * Storage size in GB. Defaults to 100. Changing it triggers upgrade (`PUT .../{account_id}`) - grow-only, shrinking is rejected by the API.
     */
    size?: pulumi.Input<number | undefined>;
    /**
     * Name of the storage. Create-only, changing it replaces the storage.
     */
    storageName: pulumi.Input<string>;
}
