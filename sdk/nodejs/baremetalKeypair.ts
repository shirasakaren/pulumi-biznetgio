// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class BaremetalKeypair extends pulumi.CustomResource {
    /**
     * Get an existing BaremetalKeypair resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): BaremetalKeypair {
        return new BaremetalKeypair(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'biznetgio:index:BaremetalKeypair';

    /**
     * Returns true if the given object is an instance of BaremetalKeypair.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BaremetalKeypair {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BaremetalKeypair.__pulumiType;
    }

    /**
     * Keypair id in BiznetGIO.
     */
    declare public /*out*/ readonly keypairId: pulumi.Output<number>;
    /**
     * Name of the keypair.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Private key from the creation response, when the API returns one. Only appears once; preserved across refreshes.
     */
    declare public /*out*/ readonly privateKey: pulumi.Output<string>;
    /**
     * When set, the keypair is imported via `POST /baremetals/keypairs/import`. When empty, the keypair is generated server-side. Create-only, changing it replaces the keypair.
     */
    declare public readonly publicKey: pulumi.Output<string | undefined>;
    /**
     * Raw JSON of the keypair item from the list API.
     */
    declare public /*out*/ readonly raw: pulumi.Output<string>;

    /**
     * Create a BaremetalKeypair resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BaremetalKeypairArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["publicKey"] = args?.publicKey;
            resourceInputs["keypairId"] = undefined /*out*/;
            resourceInputs["privateKey"] = undefined /*out*/;
            resourceInputs["raw"] = undefined /*out*/;
        } else {
            resourceInputs["keypairId"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["privateKey"] = undefined /*out*/;
            resourceInputs["publicKey"] = undefined /*out*/;
            resourceInputs["raw"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["privateKey", "raw"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(BaremetalKeypair.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a BaremetalKeypair resource.
 */
export interface BaremetalKeypairArgs {
    /**
     * Name of the keypair.
     */
    name: pulumi.Input<string>;
    /**
     * When set, the keypair is imported via `POST /baremetals/keypairs/import`. When empty, the keypair is generated server-side. Create-only, changing it replaces the keypair.
     */
    publicKey?: pulumi.Input<string | undefined>;
}
